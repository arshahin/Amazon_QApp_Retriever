# AWS Credentials Configuration Template
# Copy this file to .env and fill in your actual credentials

# AWS Access Keys (for temporary or permanent credentials)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
AWS_SESSION_TOKEN=your_session_token_here  # Optional, for temporary credentials

# AWS Region
AWS_REGION=us-east-1

# AWS Account ID (optional, for validation)
AWS_ACCOUNT_ID=120966300216

# Optional: AWS Profile (if using named profiles instead of keys)
# AWS_PROFILE=your-profile-name

# Instructions:
# 1. Copy this file: cp config/.env.example config/.env
# 2. Edit config/.env with your actual AWS credentials
# 3. Never commit config/.env to version control (it's in .gitignore)
